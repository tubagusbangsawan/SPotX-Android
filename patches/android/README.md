# Android patch profiles

Document the exact Spotify version/build, Android API range, target, expected behavior, fallback behavior and disable procedure for every tested build. Hooks should fail closed when targets are unavailable.
