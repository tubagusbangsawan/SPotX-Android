# Architecture

Termux is the control plane for detection, metadata backup, profile selection and logs. The Android runtime module is the execution plane for process detection and verified runtime hooks. Android targets are maintained separately from desktop SpotX projects.
