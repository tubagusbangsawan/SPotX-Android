# Contributing

Record the exact Spotify version/build for tested hooks. Keep hooks optional and fail closed. Run python tools/check-manifest.py. Never commit Spotify APKs, extracted DEX files, account data, or proprietary assets.
